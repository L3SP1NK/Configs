super + c
	killall gpick;gpick -pos|xclip -sel clip

super + l
	xscreensaver-command -lock

Shift_L + Super_L
	compokill;xfce4-screenshooter -r -c
		
super + shift + s
	compokill;xfce4-screenshooter -r -c

ctrl + alt + Delete
	xscreensaver-command -lock
